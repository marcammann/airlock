apiVersion: airlock.dev/v1alpha1
kind: AirlockWorkload
metadata:
  name: compose-github-checkout
spec:
  workload:
    spiffeId: spiffe://airlock.local/compose/git-checkout/component/airlock-proxy-worker
    namespace: compose
    serviceAccount: git-app
  policyRefs:
    - name: compose-github-checkout
